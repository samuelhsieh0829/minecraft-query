services:
  mc-dcbot:
    image: mc37_dcbot
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mc-dcbot
    restart: unless-stopped
    networks:
      - minecraft37_network
    volumes:
      - ./:/data

networks:
  minecraft37_network:
    external: true
